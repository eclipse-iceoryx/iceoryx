# codecov yaml
comment: false

coverage:
  range: 70..100

  status:
    project:
      default:
        enabled: yes
        target: auto
        threshold: 2% # allow coverage to drop maximum by a defined value
    patch: off
    changes: no

parsers:
  gcov:
    branch_detection:
      conditional: yes
      loop: yes
      method: yes
      macro: yes

fixes:
  - "/home/runner/work/iceoryx/iceoryx/::"
